1. sudo apt-get install filezilla #安装client
2. sudo apt-get install filezilla-locales #安装支持中文的语言包

然后就可以使用了。
如果出现./filezilla/filezilla.xml、./filezilla/layout.xml等文件不允许些或者说不能够读，可能是权限问题，讲这些文件赋予合适的权限就行了。
